::: {.content-hidden}
{{< include _macros.qmd >}}
:::

# Plug-In Estimators

## What is a Plug-In?

...

## Plug-In Bias

What's wrong with a plugin $\Psi(\hat{\p}_n)$? 

If $\hat{\p}_n$ is fit with ML we always get bias. 

Model selection: bigger and smaller models.

Validation error is bias^2 + variance. When we minimize it some bias (eg in $\mu$) remains and it translates to bias in $\hat\phi$

Bias is a problem if you want to construct CIs

The plug-in bias does not disappear quickly enough in the asymptotic expansion to be CLT-negligible 

Thus CIs etc will be wrong, even though estimate may be ok. 
--- ppl need practice for this (or example)

## Von Mises Expansion (formalizing above)

## 

<!-- What does DML do? 
For PD estimands, DML estimators are exactly the EEE/GMM estimators. Emphasizes ML and sample splitting, which is not new but needed more timely attention (cv-TMLE [2010] perhaps too soon relative to "big data" and DL). under some regularity conditions (eg continuity of phi), better describes when estimands have a "double robustness" property that can be exploited and when nusiance rates can be favorably slow. insights about rates extend to non-PD estimands. Original papers only discuss use with nonparam EIF but I think should hold as well with EIF in restricted models with appropriate and easily-satisfied regularity conditons.

@dml @dml-gmm @orth-stat-lrn

-->